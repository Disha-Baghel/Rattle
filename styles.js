const styles = {
    container: {
      backgroundColor: '#434556', // Background color
      width: 60, // Width of the container
      height: 60, // Height of the container
      borderRadius: 16, // Rounded corners
      shadowColor: '#fff', // Shadow color
      shadowOffset: { width: 0, height: 2 }, // Shadow offset
      shadowOpacity: 0.3, // Shadow opacity
      shadowRadius: 4, // Shadow radius
      justifyContent: 'center', // Center content vertically
      alignItems: 'center', // Center content horizontally
    },
    image: {
      width: 60, // Width of the image
      height: 60, // Height of the image
      resizeMode: 'cover', // Adjust image size mode as needed
    
    },
  };
  
  export default styles;